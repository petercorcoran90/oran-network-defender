apiVersion: v1
kind: ConfigMap
metadata:
  name: oran-defender-config
data:
  DB_HOST: "db-service"
  DB_PORT: "3306"
  DB_NAME: "orandb"
  SERVER_PORT: "8080"
  FRONTEND_ORIGIN: "http://frontend-service"
