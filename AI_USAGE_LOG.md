# AI Usage Log

All AI-assisted changes must be logged here before merging. See CLAUDE.md for policy.

> **Reviewer column:** add the initials of the team member who reviewed each change before
> it was committed. Entries marked _pending_ still need a human review recorded.
> AI tool used: Claude Code (Claude Opus). Branch: `front-end` (earlier work on `back-end-rest-api`).
> **Many 2026-05-29 entries are marked _(pending review)_** — they were verified to compile/build
> and were exercised end-to-end against a throwaway backend, but still need a human review pass
> before the team relies on them, per the AI policy.

| Date | Author | Area | What AI generated | How it was reviewed | Changes made before commit |
|------|--------|------|------------------|--------------------|-----------------------------|
| 2026-05-28 | Team + Claude | Project docs (`CLAUDE.md`, `ARCHITECTURE.md`, `GAME_LOGIC.md`, `AI_USAGE_LOG.md` template) | Read the project brief PDF and drafted the four shareable project/context docs | Team read against the brief | Trimmed/edited to match the team's own decisions |
| 2026-05-28 | Team + Claude | Runnable skeleton (Docker Compose + MySQL, Spring Boot/Maven/Java 21, React+Vite, K8s manifests, `.env.example`, `.gitignore`) | Generated the multi-service skeleton to get the team running | Team confirmed services start via Docker Compose | AI initially over-built full API code; team had it stripped back to a **runnable skeleton only** |
| 2026-05-28 | Team + Claude | JPA entity identifiers | Added `@Id`/`@GeneratedValue` to empty entity skeletons so Spring Boot could boot | Team verified the app starts and connects to MySQL | None |
| 2026-05-28 | Team (AI-assisted) | Game database schema (entities + repositories) | AI assisted; **team authored** the `Incident` entity in full and extended the repository finders | Team authored/reviewed directly | Substantial team authorship beyond AI suggestions |
| 2026-05-28 | Team + Claude | REST controllers + service stubs | Generated controller endpoints and empty service stubs from the team's project-goals document | Team reviewed endpoint design against their document | Endpoints chosen by the team; AI matched them |
| 2026-05-28 | Team + Claude | `IncidentResponse` DTO | Generated DTO so the API hides `rootCause` from clients (anti-cheat / security) | Team reviewed; compiled against real entity | Fixed to match entity structure (`@ManyToOne` relations, enum `.name()`); removed non-existent fields |
| 2026-05-29 | Team + Claude | Game engine (`ActionType`, `RootCause`, `EvaluationResult`, `IncidentEvaluator`, `ScoreCalculator`) | Generated the pure, dependency-free game-logic core; rules based on the `GAME_LOGIC.md` incident/action matrix | Compiles on JDK 21; **unit tests to be written by the team** | None — kept pure (no Spring/DB) so the team can test it exhaustively |
| 2026-05-29 | Team + Claude | Head-to-head schema change | Added `player_id` FKs to `network_cells`/`incidents`, `duration_seconds` to `game_sessions`, built out `ScoreEvent`, deleted duplicate `User` entity, added per-player repo finders | Team confirmed clean compile | None |
| 2026-05-29 | Team + Claude | `GAME_LOGIC.md` update | Documented the 2-player head-to-head mode and the engine (rules, root-cause→action map, scoring formula) | Team reviewed | None |
| 2026-05-29 | Team + Claude | Service layer + error handling _(pending review)_ | Implemented all five service bodies, wired the engine into `IncidentService.submitAction`, added exception classes + `GlobalExceptionHandler`, added `durationSeconds` to the create-session request | Compiles on JDK 21; **human review pending before relying on it** | Flagged for review: (1) `SessionController` request changed to accept `durationSeconds`; (2) network/incident **generation not included** (simulator's job); (3) `playerId` trusted from the request until real auth exists |
| 2026-05-29 | Team + Claude | H2 test environment | H2 (test scope), `application-test.yml`, `data.sql` seed, a context smoke test; `sql.init.mode=never` for prod | Smoke test passes on H2 | `@ColumnDefault` added later so the seed loads after schema |
| 2026-05-29 | Team + Claude | Frontend import (Claude-designed UI) | Imported the Claude-designed React UI into `frontend/`, keeping the Docker/nginx/`/api`-proxy setup | Builds via Vite | Dropped redundant generated config; deleted `vite-export/` |
| 2026-05-29 | Team + Claude | REST API client + Lobby connect flow | `api.js` client + `Lobby.jsx` (identify → create/join → match room); `App` gated on a connection | Verified create/join end-to-end vs the live backend | — |
| 2026-05-29 | Team + Claude | DTO responses + per-player API _(pending review)_ | All controllers return DTOs (fixed Jackson lazy-proxy 500s); `?playerId=` filtering on cells/incidents | Verified full flow on a throwaway instance; **human review pending** | Initial Java generation here was later replaced by the simulator |
| 2026-05-29 | Team + Claude | Frontend store wired to backend _(pending review)_ | Rewrote `store.js` to fetch/poll the backend and map shapes (recommended actions hidden); `/api/actions`; screen tweaks | Builds; manual play; **human review pending** | — |
| 2026-05-29 | Team + Claude | Ready-check + 3s countdown | `Player.ready`, `POST /sessions/{id}/ready`, lobby ready buttons + countdown; removed auto-start | Verified both-ready → ACTIVE | — |
| 2026-05-29 | Team + Claude | Python network simulator (sole generator) + ingest _(pending review)_ | New stdlib `simulator/` service; `/api/internal` ingest endpoints + optional token guard; removed `GameInitializer` | Verified seeding, mirroring, ongoing incidents; **human review pending** | Security: internal endpoints need cluster isolation / token — flagged |
| 2026-05-29 | Team + Claude | Username login (find-or-create) | `POST /api/users/login` returns the existing user or registers | Verified idempotent (same name → same id) | Impersonation possible without auth — flagged |
| 2026-05-29 | Team + Claude | Bug fix: UI not populating | Fixed `api.js` `getCells`/`getIncidents` signatures (playerId was sent as `status` → 400 → empty UI) | Verified UI populates | — |
| 2026-05-29 | Team + Claude | Cell heal/degrade on resolve | `IncidentService.applyOutcome` heals the cell on a correct fix, degrades it on a trap | Verified GOOD/CRITICAL transitions | — |
| 2026-05-29 | Team + Claude | Difficulty ramp + cascade + match timer _(pending review)_ | Simulator difficulty curve; backend cascade (a trap spawns incidents on neighbours); frontend countdown timer | Verified cascade spawns neighbour incidents; **human review pending** | Cascade can snowball on repeated mistakes (by design) |
| 2026-05-29 | Team + Claude | Cheat sheet + end-of-game screen | `GAMEPLAY_CHEATSHEET.md`; `GameOver.jsx` winner/summary screen | Verified scoring math (+140/−95/−10) and ENDED transition | — |
| 2026-05-30 | Team + Claude | 3D map + scatter + difficulty | Integrated the Three.js network map; deterministic per-session tower scatter; EASY/MEDIUM/HARD → 3/6/9 towers | Builds; verified tower counts per difficulty | `three` dep added |
| 2026-05-30 | Team + Claude | Join-by-code + minutes slider | `GET /api/sessions/code/{code}`; lobby code-entry; themed minutes slider | Verified join-by-code (case-insensitive, 404 on bad code) | — |
| 2026-05-30 | Team + Claude | Action-matrix verification | Drove all 72 incident×action combos through the backend | 0 mismatches (result/status/points) — no code change | — |
| 2026-05-30 | Team + Claude | Difficulty/incident-rate tuning | Live open-incident query, target scales with towers+time, stacking per cell, severity pool by difficulty | Diagnosed starvation as an idle-opponent artifact via DB logs | A per-player refill experiment was rolled back at the team's request |
| 2026-05-30 | Team + Claude | Bug fixes | Failed incident showed "resolved"; new game opened on last screen; signal-quality coloured red when high; cascade/forfeit/heal | Verified each fix | — |
| 2026-05-30 | Team + Claude | Ragequit forfeit + high-score table _(pending review)_ | `MatchResult` recorded on end; `GET /api/highscores`; lobby "Top scorers"; leaver forfeits (0–0 excluded) | Verified forfeit loss + table contents; **human review pending** | — |
| 2026-05-30 | Team + Claude | Remove dead UI controls | Cut Tweaks difficulty/sim-speed, Settings toggles, Acknowledge, Filters, Penalty/Health/online columns | Builds; layout preserved | — |
| 2026-05-30 | Team + Claude | configuration-status metric | `NetworkCell.configStatus` (STABLE/CHANGED/DRIFT) across entity/DTO/simulator/UI | Verified config faults map to CHANGED/DRIFT | — |
| 2026-05-31 | Team + Claude | O-RAN topology accuracy + interactivity | Reworked topology to a correct O-RAN stack with labelled interfaces; clickable component descriptions | Verified against O-RAN SC docs + Wikipedia (see ARCHITECTURE.md sources) | — |
| 2026-05-31 | Team + Claude | Simulator heal-clobber fix | Incident-free cells reset to healthy; drift kept in healthy band; incident created before metrics on spawn | Verified: every red cell has an incident; 0 stuck red-without-incident over a match | — |
| 2026-05-31 | Team + Claude | Docs + deploy fixes _(pending review)_ | SECURITY.md; ARCHITECTURE.md refresh; nginx `/api` proxy; K8s simulator Deployment + ingest-token wiring | Config changes; **not yet verified with a live `docker compose up` / `kubectl apply`** | — |
| 2026-06-01 | Team + Claude | Testing strategy doc | `TESTING_STRATEGY.md`: pyramid mapped to the brief, Testcontainers(MySQL) for integration/system, per-layer commit order with a Sonar run each step | Doc only; team agreed the plan | — |
| 2026-06-01 | Team + Claude | Phase 0: quality plumbing | `quality/docker-compose.sonar.yml` (SonarQube Community + Postgres) + README; JaCoCo in `pom.xml`; root `sonar-project.properties`; `.github/workflows/ci.yml` | `mvn verify` emits `jacoco.xml`; **all 3 CI jobs green** on the push | Local Sonar scan (runner can't reach localhost Sonar) — documented |
| 2026-06-01 | Team + Claude | Step 1: engine unit tests _(pending review)_ | `IncidentEvaluatorTest` (hand-written correct/trap expectations + full 72-combo sweep + contextual-IGNORE rule) and `ScoreCalculatorTest` (literal golden scores + time-bonus boundaries) | 121 tests pass on JDK 21; **100% line+branch on the engine package**; **human review pending** before counting as done | AI drafted; team must confirm the assertions are meaningful per the AI policy |
| 2026-06-01 | Team + Claude | Step 2: controller/API + negative tests _(pending review)_ | `@WebMvcTest` slices: `SessionControllerTest`, `IncidentControllerTest` (negative action cases), `UserControllerTest` — happy path + Bean Validation 400s + exception-to-status (404/409/400) + leak-free error body | 18 new tests (139 total) pass on JDK 21; **human review pending** | Services mocked (`@MockBean`); covers the brief's "negative tests for invalid player actions" + "errors don't leak internals" |
| 2026-06-01 | Team + Claude | Step 3: persistence/integration tests + retire H2 _(pending review)_ | Removed H2 dep + smoke test + `data.sql`; added Testcontainers (MySQL 8); `AbstractMySqlIntegrationTest` (one shared container), `Fixtures`, `PersistenceIntegrationTest` (mappings/enums/finders), `SubmitActionIntegrationTest` (correct→heal/score, trap→fail/cascade, ineffective→stays open, cross-player rejected) | 6 tests pass locally + on CI; **human review pending** | Real MySQL replaces H2; tests need Docker (CI has it) |
| 2026-06-01 | Team + Claude | Local Testcontainers fix + CI/dialect cleanup | Pinned docker-java API to 1.41 (Surefix systemProperty) for modern daemons that 400 on API<1.40; dropped explicit hibernate.dialect; bumped GitHub Actions to node24 majors | `mvn verify` green locally; CI green with zero annotations | Diagnosed via raw socket probes (`/v1.39/info`→400, `/v1.40`→200) |
| 2026-06-01 | Team + Claude | Step 4: full-flow system test _(pending review)_ | `SystemFlowTest` (`@SpringBootTest` RANDOM_PORT + `TestRestTemplate`): create→join×2→ready→ACTIVE→ingest cell+incident→submit correct action→assert score & incident RESOLVED, plus root-cause-not-leaked check. Refactored base so subclasses pick MOCK vs RANDOM_PORT; test profile `ddl-auto: create` | 145 tests pass locally (build ~21s); **human review pending** | The brief's one mandatory E2E; no mocks, real HTTP + MySQL |
| 2026-06-01 | Team + Claude | Step 5: frontend tests (Vitest/RTL) _(pending review)_ | Vitest config (jsdom + coverage-v8 + setup); `api.test.js` (URL/body building incl. the playerId-as-status regression, ApiError, 204), `store.test.js` (statusOf, Selectors, backend→UI mapping incl. root-cause-hidden, applyAction), `screens.test.jsx` (Incidents/Scoreboard display + IncidentDetail action submission) | 18 tests pass; api.js 100% / store.js 98% line coverage; LCOV emitted for Sonar; **human review pending** | WebGL map mocked in jsdom; added `@vitest/coverage-v8` (bumped vitest to 2.1.9). Playwright skipped — brief says "browser OR e2e"; backend SystemFlowTest covers it |
| 2026-06-01 | Team + Claude | Step 6: Python simulator tests (pytest) _(pending review)_ | `simulator/test_simulator.py`: archetypes match backend RootCause enum + valid severity/health/config; `severity_pool` ramp; `build_plan` repeatable per session + valid incidents; `apply_symptom`/`drift` (repeatable, healthy cells stay GOOD); `derive_health` thresholds; `elapsed_fraction` bounds + safe on bad input | 12 tests pass; `coverage.xml` emitted for Sonar; **human review pending** | Covers the brief's Python asks: generation logic, repeatable output, valid incidents |
| 2026-06-01 | Team + Claude | Step 7: SonarQube quality review _(pending review)_ | Fixed `S2819` postMessage origin (the only CRITICAL), non-root simulator Dockerfile, unused-var smells; reviewed 6 hotspots (5 pseudorandom → Safe w/ written reasons); accepted (not suppressed) ~170 prop-validation/a11y findings as plain-JS-React conventions | Gate passed; frontend still builds + 18 tests pass; write-up in `SECURITY.md`; **human review pending** | Explicitly did NOT disable rules to inflate ratings (per the brief's integrity expectation) |
| 2026-06-02 | Team + Claude | Docker Compose end-to-end verification | No code change — ran `docker compose up --build` and drove a full game over HTTP | **Verified live**: 4 containers up (db healthy), backend `/actuator/health` UP, 9 actions seeded, SPA served, nginx `/api`→backend proxy works, **simulator container seeded 6 cells + 2 incidents**, action submission changed the score | Closes the earlier "not yet verified with a live docker compose up" note. K8s `kubectl apply` still to verify |
| 2026-06-02 | Team + Claude | ZAP (DAST) hardening _(pending review)_ | nginx security headers (CSP, X-Frame-Options, nosniff, Referrer/Permissions-Policy, COOP/CORP, `server_tokens off`); self-hosted fonts via `@fontsource` (dropped Google Fonts CDN) | Re-scanned with ZAP: **0 FAIL, 63 passing**; remaining 4 warns accepted w/ reasons in `SECURITY.md`; frontend builds + 18 tests pass; **human review pending** | Branch `test-containers`; report committed as evidence |
| 2026-06-02 | Team + Claude | Game depth Step 1: investigation engine _(pending review)_ | Pure engine for hidden-root-cause investigation: `SymptomGroup` (3 ambiguous groups partitioning the 8 root causes), `DiagnosticType` (6 diagnostics), `Evidence`/`EvidenceResult`, `DiagnosticEvaluator` | 82 engine tests pass incl. **proof that full investigation uniquely identifies the cause** + investigate→fix→CORRECT for all 8; **human review pending** | Branch `feat/investigation`; engine only — no behaviour change yet (service/API/sim/UI follow) |
| 2026-06-02 | Team + Claude | Game depth Step 2: diagnostics persistence + API _(pending review)_ | `DiagnosticRun` entity + repo; `IncidentService.runDiagnostic`/`getDiagnostics` (owner/state/group guards, idempotent); diagnostics erode the response-time bonus in `submitAction`; `POST`/`GET …/incidents/{id}/diagnostics`; `IncidentResponse` exposes `symptomGroup` + `availableDiagnostics` (root cause still hidden) | 236 backend tests pass incl. 6 Testcontainers diagnostic tests (evidence/idempotency/group-guard/time-erosion/ownership) + 3 controller; **human review pending** | Branch `feat/investigation`; simulator + UI next |
| 2026-06-02 | Team + Claude | Game depth Step 3: simulator presents symptom groups _(pending review)_ | Simulator emits incidents by ambiguous `SYMPTOM_GROUPS` (shared label/severity/description/metrics per group; hidden root cause picked from the group); difficulty ramps by ambiguity (2-candidate groups, then the 4-candidate group later) | 14 pytest tests pass incl. **ambiguity proof** (causes in a group present identically) + group↔backend partition match; **human review pending** | Branch `feat/investigation`; UI next; full live verify after UI |
| 2026-06-02 | Team + Claude | Game depth Step 4: investigation UI _(pending review)_ | `api.js` run/getDiagnostics; `store.js` maps `symptomGroup`+`diagnostics` and adds `runDiagnostic`/`getDiagnostics`; `IncidentDetail` gains an Investigation panel (run diagnostics → see evidence) above the remediation actions | 22 frontend tests pass; **verified LIVE** on the rebuilt stack: HARD session seeds ambiguous "Congestion" incidents (identical cards), diagnostic returns evidence, root cause not leaked; **human review pending** | Branch `feat/investigation`; feature complete across engine→API→sim→UI |
| 2026-06-02 | Team + Claude | Game depth: deduction board (playtest fix) _(pending review)_ | Playtest said evidence didn't connect to an action ("keep trying actions until they work"). Added `RootCause.label()`; `IncidentResponse.candidates` ({cause,label,fix}); `DiagnosticResponse.implicated`; UI now shows the possible causes + their fixes, strikes out ruled-out ones, and highlights the surviving candidate's action (★) | 236 backend + 23 frontend tests pass; **verified live**: Alarms incident → CORRELATE_ALARMS confirms → board points to FILTER_ALARMS; **human review pending** | Turns guessing into deduce-then-apply; candidate set is public, the real cause stays hidden |
| 2026-06-02 | Team + Claude | Game depth: investigation budget + cost + no auto-answer _(pending review)_ | Playtest 2: "too easy — diagnose everything, click the recommended action, click race". `SymptomGroup.diagnosticBudget()` (2 for the 4-candidate group, 1 for 2-candidate — can't fully eliminate); `runDiagnostic` enforces it; diagnostics now cost real points (`DIAGNOSTIC_COST=15`, replacing the time-erosion); removed the auto-recommended ★ — the player deduces and chooses; UI shows tests-used/budget and disables when spent | 238 backend + 23 frontend tests pass; **verified live**: service-degradation budget=2 of 4, 3rd diagnostic rejected 400; **human review pending** | Forces reasoning under uncertainty; accuracy + economy beat click-speed |
| 2026-06-02 | Team + Claude | Diagnostic console — design doc | `CONSOLE_DESIGN.md`: simulated terminal where the player types authentic commands and reads output to deduce the cause; command set, sample outputs, architecture, security (emulated only), tests, build plan | Doc only; team chose defaults + authentic command names | Branch `feat/console` |
| 2026-06-02 | Team + Claude | Console Step 1: parser + renderer (engine) _(pending review)_ | `DiagnosticType` gains an authentic `command` + match prefix; pure `ConsoleRenderer` recognises a typed command → DiagnosticType and renders realistic terminal output (anomaly on CONFIRMS, clean on RULES_OUT). Emulated — nothing executed | 16 console tests pass (recognition incl. kubectl disambiguation; output differs and never echoes the root cause); **human review pending** | Branch `feat/console`; API + terminal UI next |
| 2026-06-02 | Team + Claude | Console Step 2: API _(pending review)_ | `POST …/incidents/{id}/console`; `IncidentService.runConsoleCommand` (help/man/clear free; a relevant command runs the real diagnostic via the existing budget/cost; irrelevant → free "nominal"; unknown → "command not found"); `ConsoleResponse` DTO; input bounded with `@Size(max=200)` | 262 backend tests pass incl. console integration (help/run/unknown/irrelevant/budget) + controller (output + validation); **human review pending** | Branch `feat/console`; terminal UI next |
| 2026-06-02 | Team + Claude | Console Step 3: terminal UI _(pending review)_ | `api.js`/`store.js` `runConsole`; `IncidentDetail` terminal (scrolling output log + input, command history ↑/↓, `clear`, disabled when closed) beside the easy-mode buttons; re-fetches evidence after each command so the deduction board stays in sync | 26 frontend tests pass; **verified live**: `help` lists commands, `traceroute` prints 57% loss, `rm -rf /` → command not found (nothing executed); **human review pending** | Branch `feat/console` |
| 2026-06-02 | Team + Claude | Learning-progression design doc | `LEARNING_PROGRESSION.md`: curriculum (guided→console), persistent per-user progression, teach-once modal, fix-by-command, arg-aware console, field manual, adaptive difficulty; multiplayer-fairness fork | Doc only; team chose **C+A**, arg-aware console all-the-way, build in order | Branch `feat/progression` |
| 2026-06-02 | Team + Claude | Progression Phase 1: per-user learned skills _(pending review)_ | `UserSkill` entity (learnedActions/learnedDiagnostics ElementCollections, keyed by user id) + repo; `ProgressionService` (getOrCreate, idempotent learnAction/learnDiagnostic, hasLearned*); `SkillTier` (Trainee/Operator/Engineer by count); `GET /api/users/{id}/skills` + `UserSkillResponse` | 269 backend tests pass (SkillTier 2, Progression integration 3, controller 2); **human review pending** | Branch `feat/progression`; persisted across matches; learn-on-use wiring is Phase 2 |
| 2026-06-02 | Team + Claude | Progression Phase 2a: learn-on-use + console fixes (backend) _(pending review)_ | Actions get authentic CLI commands + match; `submitAction` learns the action for the player (`justLearned` flag, persisted); the console now applies remediation commands (rrmctl/kubectl/netconf/fmcli/ticket) through the same engine + scoring; `PlayerActionResponse` carries the lesson (justLearned, actionCommand, diagnoseCommands) | 272 backend tests pass (action recognition vs diagnostics; learn-on-submit justLearned then false; console applies & resolves a correct fix); **human review pending** | Branch `feat/progression`; folded the console-fix capability in so disabling a button can't soft-lock; teaching modal + button-disable are 2b |
| 2026-06-02 | Team + Claude | Progression Phase 2b: teaching modal + button-disable (frontend) _(pending review)_ | Store fetches the player's skills each refresh (learnedActions/diagnostics/tier → state); `IncidentDetail` shows a one-time **teaching modal** (diagnose + fix CLI) when an action is first applied, and **disables learned action buttons** ("apply from the console") | 29 frontend tests pass; **verified live**: button apply → justLearned + actionCommand, skills shows REBALANCE_TRAFFIC (1/15, Trainee), console applies the fix, correct fix → SUCCESS +140; **human review pending** | Branch `feat/progression`; arg-aware console (Phase 4) next |
| 2026-06-02 | Team + Claude | Progression Phase 4: argument-aware console + man _(pending review)_ | Each command declares `requiredArgs`; the console only runs when they're supplied (substring-checked); `help` lists command NAMES only; `man <cmd>` reveals the full command + args (diagnostics & actions); a wrong attempt points to `man` without revealing the answer; running a diagnostic now also learns it | 275 backend tests pass (hasRequiredArgs; console-requires-args; man-reveals; help-names-only); **verified live** (help=names, traceroute→man hint, man traceroute→reveals, traceroute o-ru-07→runs); **human review pending** | Branch `feat/progression`; per user: help must NOT reveal the full command. Frontend unchanged (terminal relays). Field manual (5) + training mode/adaptive difficulty (6) remain |
| 2026-06-02 | Team + Claude | Progression Phase 5: field manual _(pending review)_ | `GET /api/users/{id}/manual` (server-filtered to LEARNED commands only — can't skip the curriculum) + `ManualResponse`; the Actions screen is repurposed into a **Field Manual** that fills in as you learn — diagnostics (command + what they investigate), fixes (command), tier + learned/total, recently-applied | 276 backend + 33 frontend tests pass; **verified live**: after learning, manual shows `traceroute o-ru` (Transport link fault) + Escalate (`ticket open …`); **human review pending** | Branch `feat/progression`; training mode + adaptive difficulty (Phase 6) remain |
| 2026-06-02 | Team + Claude | Progression Phase 6 (backend+sim): Training mode _(pending review)_ | `GameSession.mode` (HEAD_TO_HEAD/TRAINING); `SessionService.createTrainingSession` (one player, immediate activate, difficulty from tier — Trainee/Operator/Engineer → EASY/MEDIUM/HARD); `POST /api/sessions/training` + `TrainingStartResponse`; `SessionResponse.mode`; simulator now seeds sessions with ≥1 player | 280 backend tests pass (training activates solo at the tier's difficulty; controller 201 + validation); **verified live**: solo session created TRAINING/ACTIVE/EASY and the simulator seeded 3 cells + 2 incidents; **human review pending** | Branch `feat/progression`; difficulty-at-creation (ramps across sessions); no competitive leaderboard |
| 2026-06-02 | Team + Claude | Progression Phase 6 (frontend): training entry + solo end screen _(pending review)_ | `Api.startTraining`; Lobby "Start training" (solo, activates immediately) → enters the game directly; store carries session `mode`; `GameOver` shows a "Training complete" summary with the tier reached (no win/lose) for solo | 35 frontend tests pass (startTraining; GameOver solo branch); **human review pending** | Branch `feat/progression`; **completes the learning-progression feature (Phases 1–6)** |

## Notes / known gaps to revisit

- **No authentication yet** — `playerId`/username are taken from the request; anyone can claim
  any username and the `/api/internal` simulator endpoints rely on namespace + an optional
  shared token. The brief's "explicit authorization" still needs a real auth model.
- **Simulator is the sole generator** — a match has no cells/incidents until the Python
  simulator (its own container) picks it up; if the simulator is down, matches stay empty.
- **Cascade can snowball** — repeated wrong actions keep spawning neighbour incidents
  (bounded by the per-tick refill + `PER_CELL_MAX` stacking limit); tune if playtests feel harsh.
- **Solo testing looks starved** — incident spawning is gated by the busier player's open
  count, so an idle second window makes the active player wait. Two real players don't starve.
- **Build with JDK 21**, not the default JDK 26 — Lombok cannot process 26.
- **Testing underway (see `TESTING_STRATEGY.md`)** — Step 1 (engine unit tests) is in on the
  `test-containers` branch, AI-drafted and pending human review. Still to come: controller/API
  + negative tests, Testcontainers integration (`submitAction` + heal/cascade), the full-flow
  system test, frontend (Vitest/RTL), and the Python simulator (pytest). Per policy, AI may
  draft tests but the team must review that they assert meaningful behaviour before relying on them.
